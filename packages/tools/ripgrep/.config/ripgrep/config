# Ripgrep configuration file
# Located at ~/.config/ripgrep/config (XDG compliant)

# Always use colors
--color=always

# Show line numbers
--line-number

# Smart case searching (case insensitive unless uppercase letters are used)
--smart-case

# Follow symbolic links
--follow

# Search hidden files but respect .gitignore
--hidden

# Exclude common directories and files
--glob=!.git/
--glob=!node_modules/
--glob=!.DS_Store
--glob=!*.pyc
--glob=!__pycache__/
--glob=!*.log
--glob=!.cache/
--glob=!.npm/
--glob=!.yarn/

# Set max columns to prevent extremely long lines from slowing down search
--max-columns=200

# Show context around matches
--context=2
